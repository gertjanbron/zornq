# B179 Preflight-Report

**Datum:** 17 april 2026
**Sandbox:** Cowork Linux (Ubuntu), Python 3.10, numpy 2.2.6 / scipy 1.15.3 / cvxpy 1.7.5 / networkx 3.4.2.
**Repo-state:** commit `037cd47` ("doc(paper): apply pass-1 review fixes") post-REVIEW_PASS.

---

## 1. Git-staat

- Branch: `master`
- Commits: 1 (`037cd47`)
- **Getracked:** 5 files (alleen `docs/paper/data/b186_selector_results.{csv,json}`, `docs/paper/main.tex`, `docs/paper/tables/b186_selector_table.{md,tex}`)
- **Untracked:** ~75 items (alle `code/`, top-level config, `docs/*.md` research-notities, `docs/paper/main.pdf`, etc.)
- **Remote:** geen (sandbox-kopie; op de laptop moet de juiste origin ingesteld staan).

**BLOKKER voor Zenodo-tag-based archive**: een `git archive` zou bijna niets bevatten. Eerste stap op laptop = snapshot-commit van alle bronnen (zie `B179_BUNDLE_SPEC.md` §0).

## 2. Python-bron-integriteit

Uitgevoerd: `ast.parse()` op alle `.py`-files in `code/`.

| Metric | Waarde |
|---|--:|
| Files gescand | 261 |
| Syntax-errors | 0 |
| Null-byte corruption gevonden | 1 (`code/b7d_mpo_heisenberg.py`) |

**Sandbox-fix toegepast**: `code/b7d_mpo_heisenberg.py` eindigde met 130 null-bytes (14335 bytes → 14205 bytes na `rstrip(b'\x00')`). File parses nu clean. Last 20 bytes = `rator compact.\n""")\n`, dus file was logisch compleet, alleen disk-padding. Advies voor Antigravity: commit dit als onderdeel van de snapshot.

## 3. Test-subset verificatie

Full `pytest code/` timeout in sandbox (45 s limit, vermoedelijk traag op ILP signed spin-glass + FW 400-iter tests). Wel uitgevoerd als sanity-baseline:

| Test-module | Result |
|---|---|
| `test_b170_twin_width.py` | 41/41 passed (0.15 s) |
| `test_seed_ledger.py` | 45/45 passed (0.16 s) |
| **Subtotaal** | **86/86 passed** |

De user-rapportage noemt 39/39 `test_b159_ilp_oracle.py` + 134/134 full suite groen op laptop na de Dag-8b + review-pass fix. Sandbox-subset bevestigt dat de repo-staat daaronder consistent is.

## 4. Ontbrekende files die we moeten toevoegen voor Zenodo

| File | Status | Actie |
|---|---|---|
| `.gitignore` | ontbreekt | template in `B179_BUNDLE_SPEC.md` §6 |
| `.zenodo.json` | ontbreekt | template in `B179_BUNDLE_SPEC.md` §4 |
| `CITATION.cff` | ontbreekt | template in `B179_BUNDLE_SPEC.md` §5 (optioneel) |
| `LICENSE` | **onbekend** — check op laptop | aanbeveling: MIT (code), paper losstaand bij Zenodo als CC-BY-4.0 |

## 5. Blokkers samengevat

1. **Git-snapshot-commit vereist** voordat git-tag-gebaseerd archive werkt (zie §0 in bundle-spec).
2. **`LICENSE`-file** moet er zijn; anders weigert Zenodo vaak of krijg je `UNKNOWN` als license.
3. **ORCID + GitHub-remote** nog in te vullen in `.zenodo.json` + `CITATION.cff`.

## 6. Groen

- Python-bron: 261/261 AST-clean na null-byte fix
- Test-subset: 86/86 green
- Deps beschikbaar: numpy/scipy/cvxpy/networkx
- Repo schoon genoeg voor een snapshot-commit: geen grote binary-dumps (`results/` en `runtime_cuda/` worden correct via nieuwe `.gitignore` geëxcludeerd)

## 7. Aanbeveling aan Antigravity

Begin runbook met stap **0 git-hygiëne** (zie `B179_RUNBOOK_ANTIGRAVITY.md`). Installeer `.gitignore` eerst, daarna `git add .` (zal dankzij `.gitignore` alleen de gewenste bronnen pakken), daarna de snapshot-commit. Pas daarna `git tag paper-v1.0-2026-04-17`.
